# Driver app runtime config. Copy to .env for local development.
# Note: on a physical Android device, localhost won't reach your machine —
# use your LAN IP (e.g. http://192.168.1.20:3007) so the device can connect.
EXPO_PUBLIC_API_URL=http://localhost:3007
EXPO_PUBLIC_APP_ENV=development
